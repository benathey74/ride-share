/**
 * Trip-scoped HTTP routes (non-chat) can be added here later
 * (e.g. trip detail, cancel). Chat lives under `start/routes/chat.ts`.
 */
export function registerTripsRoutes() {
  /* intentionally empty starter */
}
