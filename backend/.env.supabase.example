# Copy to `.env.supabase` in this folder (gitignored) and set DB_PASSWORD.
# Then run: `bash scripts/migrate-supabase.sh`
# Adonis loads `backend/.env` first; this script exports these vars so they OVERRIDE .env for the migration process only (see start/env.ts).

DB_HOST=db.wzfevvzejgorhmkagxxb.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_DATABASE=postgres
DB_SSL=true
